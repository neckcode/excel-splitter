image: Visual Studio 2019

init:
  - cmd: python -m pip install --upgrade pip setuptools wheel

build_script:
  - pip install -r requirements.txt
  - pyinstaller --onefile --noconsole --name splitter excel-splitter.py

artifacts:
  - path: dist\ExcelFilterApp.exe
